#include<stdio.h>
void main()
{
  char  a;
  printf("\n enter the character");
  scanf("%c",a);
  if(A&&E&&I&&O&&Uv || a&&e&&i&&o&&u)
   printf("\n the given character is vowel");
  else
   printf(" \n the given character is consonant");
  getch();
  }
